spring:
  datasource:
    url: jdbc:h2:file:./h2/integrationtestdb
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  h2:
    console:
      enabled: true
server:
  port: 8082